// Test the function
// let userName = "john";
// let modifiedName = userName.charAt(0).toLowerCase() === userName.charAt(0) ? capitalizeFirstLetter(userName) : userName;

// console.log(modifiedName);  // Outputs: John
